// Tạo calculator với các hàm sau:
function basicCalculator(operation, a, b) {
    // Thực hiện phép tính cơ bản
    // Input: "add", 5, 3 → Output: 8
    // Input: "multiply", 4, 6 → Output: 24
}

function calculateTax(amount, taxRate = 0.1) {
    // Tính thuế (mặc định 10%)
    // Input: 1000, 0.15 → Output: 150
}

function calculateDiscount(originalPrice, discountPercent) {
    // Tính giá sau khi giảm giá
    // Input: 100, 20 → Output: 80
}

function calculateCompoundInterest(principal, rate, time, compound = 1) {
    // Tính lãi kép: A = P(1 + r/n)^(nt)
    // Input: 1000, 0.05, 2, 12 → Output: ~1104.89
}

function formatCurrency(amount, currency = "VND") {
    // Format tiền tệ
    // Input: 1234567, "VND" → Output: "1,234,567 VND"
}
